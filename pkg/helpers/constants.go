package helpers

// ContentType is a constant representing the Content-Type header.
const ContentType = "Content-Type"

// ContentPlainText is a constant representing the MIME type for plain text content with UTF-8 encoding.
const ContentPlainText = "text/plain; charset=utf-8"
